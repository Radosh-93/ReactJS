let initialState = {
	tasksToSolve: [
		{ id: 1, description: 'Test', }
	]
}

const taskReducer = (state = initialState, action) => {
	switch (action.type) {

	}
	return state
}


export default taskReducer